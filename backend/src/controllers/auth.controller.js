export const login = async (req, res) => {
  res.json({ message: "Login controller placeholder" });
};

export const register = async (req, res) => {
  res.json({ message: "Register controller placeholder" });
};

export const getJobs = async (req, res) => {
  res.json({ message: "Job controller placeholder" });
};

export const createJob = async (req, res) => {
  res.json({ message: "Create job controller placeholder" });
};
